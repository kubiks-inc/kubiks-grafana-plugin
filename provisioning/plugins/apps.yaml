apiVersion: 1

apps:
  - type: 'kubiks-kubiks-app'
    org_id: 1
    org_name: 'kubiks'
    disabled: false
    jsonData:
      apiUrl: http://default-url.com
    secureJsonData:
      apiKey: secret-key
